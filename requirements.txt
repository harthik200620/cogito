# "Cogito" — GRPO reasoning model (reference deps)
#
# NOTE: On Google Colab you do NOT install from this file. Colab uses the
# special install cell in Cogito_GRPO.ipynb (Unsloth + vLLM need a particular
# install order to avoid dependency conflicts). This file documents the stack
# for the repo / local inspection / your resume.

unsloth            # fast LoRA training + vLLM fast inference; provides FastLanguageModel
vllm               # fast generation engine used for GRPO rollouts
trl                # GRPOTrainer (the GRPO algorithm lives here)
peft               # LoRA adapters
transformers       # model + tokenizer
datasets           # dataset plumbing
accelerate         # training backend
bitsandbytes       # 4-bit quantization (load_in_4bit)
wandb              # reward-curve tracking
huggingface_hub    # push the trained model to the Hub
torch              # provided by Colab; listed for completeness
